# Shopist Python dependencies
# WARNING: intentionally pinned to vulnerable versions for SCA demo

# Web framework
Django==2.2.8              # CVE-2019-19844: account takeover via password reset
Flask==0.12.2              # CVE-2018-1000656: DoS via unexpected content type

# HTTP
requests==2.19.1           # CVE-2018-18074: redirect to non-HTTPS
urllib3==1.24.1            # CVE-2019-11324: CNAME rebinding / cert validation

# Serialization / parsing
PyYAML==5.3.1              # CVE-2020-14343: arbitrary code execution via load()
Jinja2==2.10.1             # CVE-2019-10906: sandbox escape via str.format_map

# Cryptography
cryptography==3.2          # CVE-2020-25659: Bleichenbacher timing oracle
paramiko==2.4.1            # CVE-2018-1000805: authentication bypass

# Image processing
Pillow==8.2.0              # CVE-2021-27923: buffer overflow in SGI image reader

# Database ORM
SQLAlchemy==1.3.0

# Payments
stripe==2.48.0

# Utilities
lxml==4.3.0                # CVE-2021-28957: XSS via HTML clean
celery==4.3.0              # CVE-2021-23727: command injection via broker URL
